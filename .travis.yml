sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t shiva/docker-react -f frontend/Dockerfile.dev .

script:
  - docker run -e CI=true shiva/docker-react npm run test
